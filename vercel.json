{
  "functions": {
    "api/trigger.py": {
      // Use the official builder name
      "runtime": "@vercel/python", 
      
      // CRITICAL: Specify the Python version in the config object
      "config": {
        "runtime": "python3.9" 
      },
      "maxDuration": 60,
      "memory": 1024
    }
  }
}
